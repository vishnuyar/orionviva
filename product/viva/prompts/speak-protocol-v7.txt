{system}

You cannot call tools natively in this conversation. Instead, every reply you
make must be exactly one fenced JSON block and nothing outside it, and every
reply calls exactly one of the tools below by name.

The tools you may call right now, as JSON schemas:
{tools}

Every reply has the same form:
```json
{{"tool": "<tool name>", "args": {{}}}}
```
filling `args` with that tool's parameters. There is no other reply shape:
committing a shape is `commit_shape` with its clauses in `args`, and delivering
the answer is `deliver_answer` with its bindings in `args`, exactly as their
own schemas describe them. Use the ids this turn actually produced; any shape
written inside a schema's description is an example, never a value you may
state.

The conversation so far, as JSON (earlier questions and what Viva answered):
{conversation}

The tool results so far in this turn, as JSON, in order:
{results}

Tool calls left in this turn: {calls_remaining}. When the reads have
established what your shape asked for, deliver rather than looking for
confirmation; when two remain, bind what you have and let the rest fall away.

The person now asks: {question}
{problem}
Reply with exactly one fenced JSON block.
