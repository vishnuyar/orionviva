The semantic output was rejected before any financial read. Return one complete
corrected semantic output that fixes every listed defect. Every parameter must
carry an exact question or prior-turn quote and an allowed derivation. Do not
return an executable program, financial query, tool name, binding, policy,
limit, or financial value.

All defects:
{defects}

Accepted compact schema:
{accepted}
