# TODO

> ## ⛔ HISTORICAL RECORD — do not read this as current
>
> This was the working handoff during the desktop build. It preserves findings,
> witness requests, old suite counts, and delegated-run context, but its branch,
> work-order, and capability claims are closed or superseded. Use the current
> [implementation status](../user-interface-implementation-status.md) and
> [capability gaps](../backend-capability-gaps.md).

_One line per item. Internal working aid, gitignored. Narrative belongs in commit messages and docs — this file is a list of **what is left**. Completed work lives in `git log`; it is not repeated here._

**Where we are:** the figure and its citation — work order item 2 — is built across three lanes, verified in fresh contexts across three Checkpoint 2 rounds, accepted by the **Director** under the delegated-run warrant, and sits at the commit gate on **`run/advisory`**. The overview's picture is composed by the backend: one net-worth figure per currency with the day it is good for, one reviewed sentence per figure saying how far it reaches, a per-figure evidence control named by the amount it shows, an account no currency can be placed for named at the panel, and a currency whose total is kept back saying so instead of vanishing. Nothing about it is composed by the interface, and the panel counts accounts and never names them. Suite: **1571 passed, 1 skipped, 2 xfailed**; core, merchant and bench **215**; desktop **424 passed over 31 files**; `tsc -b --noEmit` clean. Gates, all exiting 0: boundary at 153 self-checks over 44 production modules, 138 pack sentences shipped by `pack-v19` and 76 modules read; style at 66 self-checks over 5 stylesheets and 48 tokens; `check_surface_contract.py --check` and `generate_overview_parity_fixture.py --check` both current, and `surface-v1.json` never moved. **`docs/surface-charter.md` and `docs/rules.md` are held out of the commit**: Vishnu authorised those edits and not their commit, and relayed authorisation is not authorisation.

**`disabled` is now reserved for nothing, and this file said otherwise until now.** The old VOICE-135 — `disabled` for a control not available here at all, `aria-disabled` for one busy — is retired. A control the screen cannot perform does not render, which is VOICE-136's business, and where its absence changes what the person should do next, one sentence says so, which is VOICE-137's. The busy half survives verbatim, third assertion included. `Documents.tsx:13` turned from this rule's exemplar into a violation with no line of code changing, and the capture cycle closed it: the attribute is in no JSX under `desktop/src`, the checker holds it out of the whole tree, and every busy control carries `aria-disabled`, keeps its focus and refuses the second press in words. The contradiction table still holds **twelve** rows. VOICE-135's stays because `by-review` is the highest a rule held only by a Node gate and TypeScript tests can reach, and moving a state word is Vishnu's.

**The Director is in the contract, and its instruction set still is not.** `.claude/agents/director.md` is gitignored, so `WORKFLOW.md` now points at a file no clone, no build and no checking role can read — more pressing than before, because the role may now authorise a `wip(...)` commit to a delegated run branch. Publishing it is its own cycle with a scrub pass, listed below.

**Nothing in the charter is enforced and nothing may be.** These rules are about the interface, whose tests are TypeScript, and the rule index collects test names by parsing Python. `by-review` is the ceiling until something teaches the guard to read the desktop suite; none of the six has reached even that, because each is contradicted or unmet.

## Out of the figure-and-citation cycle

- **Five of the picture's paths are exercised by the parity fixture and by nothing else, and the one real vault cannot reach any of them.** The per-currency arithmetic above one currency, a withheld total, an account no currency can be placed for, the second arm of the reason vocabulary, and citation scoping per figure. Witness W-2 ran on the real vault and came back one currency, nothing unvalued, nothing withheld, nothing unplaced. **`settled_by` is structurally unreachable there** — the envelope ties a settling sentence to a refused gap and that vault declares no gaps — so **E-19 cannot be settled by any vault that exists.** The code does what was ruled; nobody has seen these work for a person. Recorded in the status document at `product/viva/surface/overview.py#_picture`.
- **Nothing visual has been seen on a running window, and E-20 produced the cycle's most serious finding anyway.** Every layout claim came from a tree rendered in headless Chrome and measured pixels. Recorded in the status document as a refusal, because whether type reads correctly in the application's own window is a claim about a machine nothing here observes.
- **E-23: the app names a display face eleven times and ships it nowhere.** `"Playfair Display"` across `shell.css` and `surfaces.css`, no `@font-face`, no font file, no link in `index.html`, and the face is not installed on this machine — so the flagship figure renders in Georgia, whose old-style numerals descend, and the figure's underline is cut around the descenders and **reads as a strike through the number**. The style gate asserts the family contains `serif`, which the fallback satisfies, so it passes on the fallback and cannot see this. Second time this cycle a pre-existing defect was the direct cause of a new one. Recorded in the status document at `desktop/src/styles/surfaces.css`.
- **F-1: the one caveat the overview declares is a Python literal and reaches no screen.** `MIXED_VINTAGE` in `ledger_tools.py` is not a reviewed pack sentence. The panel's `caveats` field is now complete and correct — it carries what every read the composer made declared, which the balances-only gather was dropping — and nothing renders it. **The first cycle that renders it moves this sentence into the persona pack before it does**, or the flagship figure's own disclosure ships as words nobody reviewed. That cycle also settles what two reads declaring identical words should do; they are carried as two entries today.
- **F-2: two true sentences one line apart read as a contradiction.** The panel says *"Every account you hold is counted here."* directly above *"One document you have added is not counted in this total."* — accounts against documents, which a person will not carry across one line. The point declares `complete: False` and the boundary `whole: False, unposted: 1` while the panel says the all-accounts sentence. On a one-currency picture *"and nothing held in any other currency"* manufactures a doubt about an empty set.
- **Q6: the panel says 7 of 9 over eight cards, and reconciling takes four hops ending two clicks deep in a drawer.** B3 is discharged — the unplaced account genuinely reaches a person now — but the door is the wrong one. Naming versus counting was ruled *counts, always* at the panel and the durable ground is in the architecture document; how a person resolves a count against the cards is not solved by it.
- **The bundled sample demonstrates none of the states this cycle built.** The product's shopfront shows a shape the product no longer has. Named outside the fence rather than fixed.
- **Ruled and deliberately not built: the ordinal's words move into the pack** as `source_place_among_same_name`, *"{position} of {total} with this name"*, with the `·` idiom staying in the component. Held back at the fourth Checkpoint 2 because the tree had already moved under a measuring role twice and the rendered output is identical either way. **The real fix behind it is filed and unbriefed: a name the read supplies** — a date, a period, an issuer — so six statements are told apart by what they are rather than by their position in one read's order. The ordinal is deliberately not pinned: a stable number that is still not the document's name impersonates an identity.
- **`_held_in` has an untested branch.** Where a line and the account disagree about currency, the line wins silently. Lane 1 could not construct the disagreement, so nothing gates it.
- **E-24: condition 26 belongs in `WORKFLOW.md`, and its mechanical half mostly cannot work.** The standing question has always been asked about documents; six comments false about the code beside them were found in this cycle alone, and comments travel with the code where documents do not. A mechanical sweep for comments naming removed or renamed code found nothing here, **and that is not reassuring** — five of the six were the other kind, naming things that all still exist while asserting a reason that stopped being true. What caught them was going to the places the ground was written down. The precondition is that a ruling superseding a ground states the superseded ground in its own words, so the old justification is a search string. Limit 11 stops at documents; extending it is Vishnu's.
- **E-21, in lane 1's words: stop deriving.** Every divergence found in the run's own memory this cycle was in something derived from a source rather than the source — a compressed ruling, a restated brief, a stale extract. The ask is not *keep the records in sync*; it is that a brief point at the record instead of restating it. Five failures of the run's own memory were found and corrected visibly rather than repaired in silence.
- **A factor of fifty nobody was measuring.** The composed overview read is a median 15.4 ms against a 250 ms budget on the real vault, and `Vault.open` is 0.78 s — about 50× the read, outside the budget's scope. Anyone reading "15 ms overview" as first paint is wrong by that factor.
- **`docs/surface-charter.md`'s VOICE-140 count is now off by three, and the file is held out of this commit.** It says 148 raw `font-size` declarations, 30 in `shell.css` and 118 in `surfaces.css`; the tree measures 30 and 115. The charter carries exactly one changed line, which is Vishnu's own, so nothing was added to it. Correcting a measured Code line travels with his word on those two files.
- **A second test name dates itself to a cycle.** `picture.test.ts:199` opens `describe("the rules this cycle changed beside the picture")` beside `contrast.test.ts:168`'s, and `Documents.test.tsx:246` names a cycle in an `it` title. A test name is executable text and the Steward does not edit code; they want one line in whichever cycle next opens those files.
- **`pack-v19` carries `pack-v17`'s comment in two of its three files.** `phrasings.json` and `tone.md` are byte-identical copies, so each carries a `_comment` reading *"pack-v17: …"* and closing *"add pack-v18 instead"* — frozen advice naming the wrong successor from inside v19. Same shape in v18. Correcting it moves the pinned digest in `versions.json`, which is an artifact change rather than a comment pass.
- **The style ratchet is looser than it was.** The gate reports 11 below the recorded ceiling on `surfaces.css` and 1 below on bare icon sizes, and refuses to tighten itself. `--write-baseline` is still run by no role in a delegated run.
- **Twenty-four escalations go up with this cycle.** They are in the run's own record under `briefs/`, each written to be actionable without reading anything else. None may be closed in a delegated run.

## Out of the capture-and-park cycle

- **Ten Witness cases, none of them costing a model call, all of them Vishnu's to authorise.** They are listed in the cycle's own record under `briefs/`. **W-1 is the one that matters most: the drop gesture on a packaged build, which nobody has watched.** No role in a delegated run can watch a file land on a window, so the Director took the drop half explicitly on structure — the drag-drop configuration assertion, the boundary fence and the self-checks stand in for the gesture — on the ground that if the gesture never fires in a packaged build the failure is *nothing happens*: no wrong number, no lost file, no sentence to a person that turns out to be false. That ground does not extend to anything that tells a person something.
- **Three interface findings deferred with the Director's knowledge, each cheap and none of them a defect in what shipped.** (1) A live row is labelled by the filename the vault recorded, and the panel that row opens is still headed by the *kind* of document — a row reading `quarter-close.pdf` opens a panel headed `statement`. (2) In the steady state the panel's reading sentence renders in the receipt's slot with no title above it, so the sentence a read wrote and the sentence a capture answered with are dressed identically. (3) The capture invitation reads weaker than the receipt beside it, measured at **3.42:1** by the interface review; the receipt's own thin pair is 4.536:1.
- **The colour work the token set does not carry: there is no accessible warning ink and no accessible warning ground.** `--rust` measures **4.13:1** on the panel ground, under the 4.5 text floor, and is usable as a border and not as words. `--muted` measures **4.20:1** on `--panel` and **4.536:1** on the page ground the capture panel actually paints. The invitation's **3.42:1** is the third figure. The colour cycle starts from those numbers rather than from a fresh eye.
- **The thinnest contrast margin this build ships is 4.536:1, a headroom of 0.8% over the floor — not the 4.563 and 1.4% that travelled.** The four page grounds resolve to two luminance pairs; three of them give 4.5634 and the fourth gives 4.5355, and the friendlier of the two is the figure that got repeated. The gate asserts against all four, so nothing unsafe ships and no test moves — what moves is the number anybody quotes. The Director withdrew its own 1.4%.
- **The contrast gate's mode guard needs wider reach.** It holds the rendered palette apart from the dark palette by finding the qualified `:root` by its shape, which works for one alternative mode and not for the general case of a token defined under several selectors.
- **`blockOf` resolves a selector by file order rather than by the cascade.** It takes the first rule whose text matches and reads to the first `}`, so a selector declared twice, or a rule inside an at-block, is read as whichever came first in the file rather than as whichever wins on the screen. Sound for the stylesheets it reads today and wrong the day one of them repeats a selector.
- **The boundary checker now bans a regular-expression literal under `desktop/src`, and the ban has a return condition.** Its scanner runs with no parser above it and stops advancing on a literal, so patterns are written as `new RegExp`. It retires by teaching the scanner the construct, never by exempting a file. Recorded in the status document so it does not live only in a comment.
- **`--write-baseline` is not run by any role in a delegated run, and the ratchet is one notch loose because of it.** The style gate records 22 bare icon sizes where the tree has 21; it reports the slack and refuses to tighten itself. Builder 2 deliberately left it under an open lane — a lane that has since closed, so the next cycle in the desktop tree regenerates the baseline and takes the notch.
- **Batch capture and the receipt's missing time reference both belong to item 3.** A gesture carrying more than one document is refused in the window before anything is sent, because one reply frame carries one answer and this build has no channel that can report several as they happen. The receipt says what became of a capture and never when, for the same reason: there is no job to ask. Both are bought by the job registry and the progress channel.
- **The brief's own `waiting` row was not built, and that is a decision rather than a shortfall.** §6 mapped the engine's `held`, `awaiting`, `conflict`, `gap` and `identity` results onto `waiting` so a terminal-written vault would render. The handler maps the two actions its own route can reach and raises on everything else, on the precedent that a reply is never read as the nearest word. Recorded in the status document beside `ActionOutcome`.
- **Process finding: the brief said copy would be ruled rather than composed, and three sentences were composed and then ratified.** §9 exists precisely so a Builder does not invent person-facing wording. Two refusals the brief owed and did not propose, and one amended sibling, were written during the cycle and approved after the fact. The approval is real; the ordering is what the section was written to prevent, and it went the other way on the first cycle that used it.
- **The staging-dependent documentation guard fired again, on the same mechanism as last cycle.** `test_a_test_a_rule_cites_is_a_test_that_exists` reads the tree through `git ls-files`, so a rule citing a test in an untracked file is red until the Steward stages it. Two Verifiers independently reproduced 1508/0 in a scratch index. The item further down asking for a WORKFLOW line or a guard that reads the index as well as `ls-files` now has two occurrences behind it.
- **A test name dates itself to a cycle.** `contrast.test.ts` opens `describe("what a person can read on the screens this cycle authored")`. A name is executable text and the Steward does not edit code; it wants one line in whichever cycle next opens that file. **The figure cycle added two more of the same shape**, listed in its own section above.

## Out of the re-sequencing cycle

- **The work order is the run's build order from here, and its first item is capture-and-park.** `docs/the-work-order.md` re-derives the sequence from the gap ledger's priority list under the stranger-first ruling. It holds no priority order of its own, declares no rule, and carries an acceptance class rather than acceptance criteria for every item. Its own closing section makes it **void rather than amended** if the gap ledger's order moves — so a later cycle that moves the ledger re-derives this document instead of patching it.
- **The very first build cycle parks a question before it builds anything.** Capture-and-park meets the fifth reserved interface unknown immediately: whether the review, activity and documents surfaces read through the surface module directly or through the conversation's own block, which the architecture document explicitly declines to take. The documents composer is the first module that has to choose. Second-lane work — the direction site first — fills that wait so the run does not idle.
- **Vishnu's queue, opened by the Director and not rulable by it.** The two charter corrections below; Witness windows and their budgets (nine named across the work order, five of which cost money); filing the vault-open defect; any voice path reaching a remote provider, which needs a decision record and a promise check before it is designed; and the five reserved interface unknowns. None may be closed in a delegated run.
- **The charter is now false in three places, and the work order names two.** (1) *"Nothing in `docs/backend-capability-gaps.md` is edited, because it is already correct as written"* — the ledger is accurate about the code in every symbol claim and wrong about **itself** twice: it says the desktop bridge client declares one fewer method than it does, contradicting its own review entry two paragraphs earlier, and its Open section says *no other document names it* when the reading guide, the charter and now the work order all do. (2) `## What this charter leaves owed` — its first bullet is this work order, and the list is introduced by a count of **four**; entry and count both go false the moment the document is tracked. (3) **Not named in the work order:** `### What is owed, and whose it is` at `docs/surface-charter.md:402` says *"It is owed as its own design cycle"* about the same work order, and goes false with the other two. The work order's escalation bullet therefore says *two sentences* where three passages are affected. All three corrections are Vishnu's alone; editing the charter was out of this cycle's fence.
- **`WORKFLOW.md`'s warrant limit 8 loses one of its grounds.** `WORKFLOW.md:196` reads *"The charter does not ratify it and the re-sequencing cycle is owed; a stand-in may not rule on the strength of an order nobody adopted."* The prohibition itself stands untouched — the work order carries no label from the outside review and draws on none of its item order — but the clause *the re-sequencing cycle is owed* goes false with the charter's two. Rewriting the grounds of a warrant limit is a design decision about the loop's own contract, not a Steward's docs edit, and `WORKFLOW.md` was outside this cycle's fence.
- **A gate gap handed forward: an anchored address in a non-anchor row is invisible.** `_ANCHOR_ROW` in `product/tests/test_docs_track_the_code.py:709` matches only a row whose first cell is a backticked direction from the five-word grammar, so a `| **—** |` row carrying an address elsewhere in the row is never evaluated. The work order carries exactly one deliberately non-resolving address of that shape and says so in its own text. Belongs to the symbol-resolution guard the gap ledger's Open section already schedules.
- **For the Director, open and deliberately not fixed: the unwitnessed marking is scoped to one paragraph.** The work order marks its *what already exists* claims as taken against a scratch vault and unwitnessed, but two neighbouring paragraphs in the same section carry behavioural claims with no marking — the served and refused operations, and the progress frames falling through. The last Verifier judged them weaker than the five marked ones, because neither needs a real vault, so nobody puts a wrong figure in front of a person on their strength; and outside the six accepted edits, so a decision rather than a defect.
- **Two unreferenced loose objects sit in `.git/objects`.** The last Verifier staged the document into a throwaway index to run the documentation guard against it. The blobs are unreachable and inert and `git gc` removes them; it declined to run `gc` because that mutates the repo, and so did the Steward.

## Out of the charter cycle

- **Voice is not deferred, and the plan has a hole it does not know about.** Vishnu rejected that row of the hide-list. The architecture document's *first public experience* sentence stays true and uncorrected; VOICE-34 (text and voice in one session, every spoken reply mirrored in text) stops being a return condition and becomes work that is owed. The outside review planned for voice nowhere, so voice and the mirrored-text provenance design VOICE-34 requires are **owed and unbriefed**. No stand-in may close that hole by reading the review's silence as a decision.
- **`.claude/agents/director.md` gets its own publication cycle, after a scrub.** It is gitignored today and `WORKFLOW.md` now names it, so the warrant points at something nothing tracked can read. Publishing is publishing: it takes the issue gate's treatment and a scrub pass rather than a ride-along.
- **The tracked role files still say the checkpoints belong to Vishnu alone.** `WORKFLOW.md` was corrected at all three sites; the nine role files under `.claude/agents/` were not, and they are tracked. `fact-checker.md:10` and `:87` say *Checkpoint 1 is Vishnu's*; `steward.md:3` and `:68` and `interface-designer.md:96` say only Vishnu says commit; `builder.md:8` and `:18` say Vishnu approves and amends the brief. Each is now false in a delegated run. Left alone deliberately: they were outside this cycle's fence, and rewriting what a role file says about who rules is a design decision rather than a docs edit. It travels with the Director publication cycle, which is already opening that directory.
- **The vault-open defect: a mistyped path silently hands the person a brand-new empty vault.** `product/viva/vault.py:31` calls `directory.mkdir(parents=True, exist_ok=True)`, so `Vault.open` on a wrong path answers `{"state": "opened"}` rather than failing. Found by the Fact-checker while refuting the review's claim that one failure code covers wrong passphrase, missing directory and corrupt vault alike — two of three are true, and a missing directory is not a failure at all. **A Reporter intake on Vishnu's word only**, since filing is publishing.
- **Two counts in `docs/rules.md` are wrong, were left wrong on Vishnu's ruling, and hand-correcting them only resets a clock.** `:635` says `56 rules.` where 55 is actual; `:788` says `135 of 483` where 134 of 485 is. Both were already wrong at HEAD by the same 1 and 2, and every delta this cycle applied is correct. Nothing holds these numbers: the guard's `_COUNTED_HEADING` matches only `## Heading (N)` and never a bare `N rules.` line, so a hand-fix drifts again on the next cycle. **The fix worth having is the check**, not the correction.
- **A Builder who creates a new rule-defining document cannot leave the working tree green.** `product/tests/test_docs_track_the_code.py:42` builds its corpus from `git ls-files`, so a new untracked document is invisible to the guard while the already-tracked `docs/rules.md` is not — `test_every_rule_a_document_defines_is_indexed_exactly_once` fails until the file is staged, and Builders do not stage. It went green here the moment the Steward staged all six paths. Worth a WORKFLOW line, or a guard that reads the index as well as `ls-files`.
- **Nothing compares a `rules.md` index row's *name* to its block heading, and there is already one disagreement.** The guard holds id, state and document; the name cell is held by attention alone. Three name cells changed by hand this cycle (VOICE-135's, in both lists and the contradiction register) and a one-off comparison confirms all three are right — but the same run found `rules.md:105`, where SPINE-2 is indexed as *Every design doc and ADR names the invariants it touches* while its block in `docs/README.md` is headed *An invariant is cited by the rule that bears on it*. Pre-existing and untouched by this cycle. A guard comparing the two would be a few lines and would have caught it.
- **`WORKFLOW.md` and the charter are coupled by nothing a test can see.** `WORKFLOW.md:33` and `:174` link `docs/surface-charter.md`, and `docs/rules.md` and the charter are hard-coupled by the documentation guard — but the contract and the charter are not. Commit one without the other and the suite stays green with two dead links, one of them inside the sentence defining the ceiling of a stand-in's authority. This cycle lands all five paths together; the general hazard stands.
- **The architecture document's seven-destination map and VOICE-136 pull against each other, and neither is wrong yet.** `user-interface-architecture-and-delivery.md:267` maps seven destinations and what each holds; VOICE-136 says navigation is a projection of the registry and a destination appears only when a surfaced capability claims it and its live read is served. The map describes what a destination holds when it exists rather than when it renders, so nothing is falsified — but whoever builds the registry cycle's gate reconciles the two, and the `account`/`accounts` vocabulary split is the first thing in the way.
- **`CLAUDE.md` says five roles live in `.claude/agents/` and `WORKFLOW.md` now describes ten.** It was already wrong by four before this cycle — the Fact-checker, Interface Designer, Witness and Reporter all predate it — and this cycle widened the gap by one. `CLAUDE.md` is gitignored and is Vishnu's own file; nobody else edits it.

## Out of the loop-reaches-the-screen cycle (Brief F)

- **The pending count a person can now move diverges from the pending list nobody can open.** The Review screen's set-aside summary moves when a person declines, and it is `open_questions()`'s `pending.count`. **A7** below is the divergence: `pending_questions` omits the `as_of` default its two siblings have, so a declined expectation question is counted and absent from the list. A7's "no production caller today" is still true of `pending_questions` — the list has no screen — but the half of the divergence a person sees now moves under their own hand. Whoever opens the set-aside list closes A7 first.
- **`viva.review.answer` is declared and served by nobody, and that is the honest state rather than a defect.** The operation appears in the table because the registry declares the action; the sidecar registers no handler, so it refuses with `operation_not_allowed`, and the interface offers no control that would call it. It stays that way until the configuration cycle.
- **The configuration cycle is now a prerequisite rather than an adjacent nicety, and it is unbriefed.** The desktop application configuring its own model, locale and currency, with egress consent visible and the `settings` destination finally claimed by a capability. Honest free-text answering needs a model, a model needs configuration, and configuration has never existed in this application.
- **The interface's craft gap is half closed.** The token file now holds a type scale, a spacing scale, radii, motion durations and easings, a reduced-motion block, icon sizes and a dark palette nothing turns on, and a gate holds a stylesheet written against that set to zero raw values while ratcheting the two older ones. What is left is the migration — 215 raw values in `shell.css` and 792 in `surfaces.css` as the gate counts them — and the keyboard half of VOICE-140, which has no check at all. Dark mode is defined and not activated on purpose: switching it on with the older stylesheets unmigrated produces a half-painted screen for every reviewer on a dark OS, and no role in this run can look at one.
- **The receipt rename is ruled and unexecuted.** VOICE-134 is `unmet`: the interface says *evidence* everywhere — badge, drawer, target resolver, link — and the site promises *receipt*. It is the Interface Designer's first assignment and travels on its own, never beside a change to behaviour.
- **Set aside, propose, confirm and correct are still unconnected**, and `ActionOutcome`'s `proposal` and `stale` still have no producer. `waiting` is reachable through the mapper and no verb this build serves produces it. The agent-proposes-human-disposes vocabulary gets its own cycle rather than a ride-along.
- **Rule blocks in `docs/user-interface-architecture-and-delivery.md` cite line numbers that moved, systematically.** VOICE-103 says `models.py:27` for `FigureView` (81), VOICE-105 says `:10` for `PanelState` (12) and `:44` for `FigureView.__post_init__` (100), VOICE-106 says `:71` for `ActionOutcome` (140). Pre-existing and not made false by this cycle; found in its docs pass and left alone, because a hand-fix of one file is the wrong repair. The right one is the symbol-resolution guard `backend-capability-gaps.md`'s Open section already schedules, extended from `path#symbol` to `path:line`.
- **Whether `MAY_CALL_MODEL` should structurally imply `MAY_EGRESS`** is still open, and now has a second instance: `review.questions` declares both alongside `documents.ingest`. Two capabilities is not an argument; the general question is unchanged.
- **The desktop application still runs against unruled build scaffolding.** `desktop/src-tauri/gen/`, `desktop/src-tauri/icons/` and `desktop/src-tauri/Cargo.lock` are untracked and were produced getting the app to run; the icons are placeholder PNGs generated past a missing-asset build failure, not artwork anyone chose. Vishnu rules on them separately, and nothing in this cycle stages them.

## Out of the gates-and-the-citation cycle

- **The page a figure was printed on is not delivered, and it is a design gap rather than an omission.** Per-part provenance does not exist in the read layer: `composed_values` carries the document for the cash term only, so the surface cannot tell a composed figure from a simple one without inferring from the shape of the data, which this project forbids. The page is therefore withheld from **every** account, including the seven where it was right. Click-through to the document works. Closing this means the read recording where each part was printed; it is a cycle, not a patch.
- **A read-level caveat is computed, serialised and then dropped — and the one thing it said now reaches a screen another way.** `OverviewData` still has no field for it, and the caveat string itself still reaches nobody. What it carried was that a total rests on measurements taken on different days, and the picture now says that fact on each of its figures, in the pack's own words and per currency, so the **disclosure is not lost and the vocabulary is not shipped**. Rendering the caveat itself is deliberately not done: it is a Python literal rather than a reviewed pack sentence, which is the channel already queued below. Owner: that channel.
- **Nothing computes staleness.** A dormant account shows a ten-month-old date and says nothing about it, and the parity fixture now carries exactly that shape so the day something computes it, there is a case waiting.
- **`spending_by_currency` is still the known-wrong number**, recorded and not fixed; `docs/rules.md`'s MON-6 row points at its new address. This is the unresolved **R5** already listed further down — same item, new address.
- **Three gaps recorded with no owner.** The Rust host has no operation allowlist and is a transparent proxy. The native folder picker's behavioural contract — folder-only, and cancellation distinct from a failed host call — is held by nothing after its text assertions were deleted; it belongs to the Node checker. And nothing in this repository reads a CI *run's* result, so a job that is declared, loadable and never triggered looks identical from inside the tree — which is precisely the state the workflow was in.
- **`bridge.open_vault` bypasses the allowlist and the protocol check.** An allowlist-of-one expressed as a branch before dispatch, on the one operation that carries a passphrase. Payload is fenced to a directory and a passphrase, so it is not an open door. Recorded by Vishnu's ruling, fixed in its own cycle.
- **Two xfail reason strings name a brief, and briefs are gitignored.** `test_synthetic_statements.py` and `test_synthetic_corpus_4y.py` are `xfail(strict=True)` with reasons naming "Brief B — The Gates That Cannot Fail", which resolves to nothing any reader of this repository can open. The Director required an xfail reason to name its owner; the standing rule says a tracked file may not point at `briefs/`. The two rules collide and the collision is unresolved. Changing the string is a code change and belongs to a cycle.
- **Both those tests are broken for a reason nobody owns.** Each invokes the generator through an absolute interpreter path under a home directory (one of them a coding agent's private cache), and the generators read their catalogs from absolute home-directory paths. They have never been able to pass outside one machine. Marked, not repaired.
- **Whether `MAY_CALL_MODEL` should structurally imply `MAY_EGRESS`** is open. `documents.ingest` now declares both; the general question is not settled.
- **`Vault.open` pays two independent scrypt derivations under one passphrase**, one for the ledger and one for the raw store — a size-independent ~0.37 s floor paid even on an empty vault, roughly 85% of the open. Whether two are necessary has never been examined. It is also the part of vault-open that can never report progress: a memory-hard derivation has no intermediate state by design.
- **The `review` surface read takes ~3,113 ms on a synthetic 50k-event vault, slower than `Vault.open` at ~1,844 ms.** The builder assembles every question family over the whole projection and ranks before applying the caller's limit, so the page size bounds nothing. The vault is 50k uncategorised transactions on one account — near worst case — so this is an upper bound for that shape, not a measurement of a real vault.
- **Three progress-channel defects are recorded for whoever builds ingest.** The request id is carried inverted, so a later read's events are stamped with the vault-open's id; `job_id` silently defaults to the literal `"surface-read"`; and `test_progress_event_frames_are_json_safe_and_preserve_order` exercises the frame encoder rather than the progress contract its name claims.
- **Four documents still have no reading-guide slot:** `README.md`, `TODO.md`, `phases.md`, `account-connection-research.md`. Three were placed this cycle. `VISION.md`'s missing slot is a separate item already listed below and is still open.
- **Two files carry two briefs' edits and cannot be attributed by filename** — `product/viva/surface/__init__.py` and `product/viva/surface/fixtures/surface-v1.json`. Separately, `desktop/scripts/check-ui-boundaries.mjs` gained one named allowed edge from the citation brief, in a file the gates brief was explicitly forbidden to touch. Recorded for the Director.
- **The scrub finding that blocks the commit is Vishnu's alone.** See the handoff; nothing here touches it.

## Out of the reachability + wording-tail + one-account cycle (R7 + R8 + R9)

- **R2 — a currency filter narrows accounts, not values, and one sentence it produces is false. Owner: a brief of its own, *what a currency filter narrows*.** A currency filter selects accounts by the currency they are **declared** in, so a figure over the INR half of a USD-declared brokerage account can be met by *"That counts only what is held in USD"* — a false scope sentence in reviewed words. Second half of the same item: two figures over disjoint halves of one account carry **byte-identical** boundary declarations, which `_boundaries` dedupes to one sentence and the entity join cannot tell apart — precisely what the `BY_CURRENCY` cut existed to prevent, and unavailable here because `bounded` refuses two entries on one axis. Both are gated behind a multi-currency brokerage account, which today's `post_brokerage` cannot produce; neither is caused by this run. Not patched deliberately: deciding what a currency filter narrows is a decision about the closed narrowing vocabulary, and any patch taken before it either deletes a true narrowing or hard-codes a comparison the real fix removes.
- **Refusal tags are an open set of free machine strings. Owner: a candidate cycle, unbriefed.** A tag is minted at the `refusal()` call site and validated by nothing; only `SPEAKABLE_REFUSALS` is closed. That is the shape the standing closed-vocabulary rule exists to prevent. Surfaced by G1, where `matching_unsupported` was minted inside a fence that forbade a new refusal tag; the Director struck the fence phrase's reach to non-speakable tags rather than bounce a correct change.
- **R7-9 is partially delivered and nobody may record it otherwise.** `LIST_MOVEMENTS_PARAMS` got `required: ["filters"]`, not the brief's stronger *at least one of these five*, which `registry._validate` cannot express — it supports `properties`, `required`, `type` and `enum` and nothing else. So `{}` is refused at validation while `{"filters": {}}` and `{"filters": {"currency": "USD"}}` still cost a call and are refused by the read. The stronger rule survives in prose in `tools-v13`/`tools-v14`. Closing it means changing the validator, which was outside the fence.
- **The coverage sentence is silent on every mixed answer, and that is the ruled trade.** A stated figure counts towards the answer's reach only where its boundary declares exactly which accounts it covers; only the balances read declares accounts today. So an answer pairing a per-account balance with a spending total, a category total or a count says nothing about how far it reaches. Making other reads emit an accounts block is forbidden — it would put a count over a set nothing can list back in by the front door. Reversing this means giving those reads a real accounts declaration, which is a cycle and not a patch.
- **R8c is queued and unbriefed:** the caveat channel — caveats as person-facing literals in Python rather than reviewed pack sentences, a caveat scoped to a read rather than to a figure, and the label-cap caveat. The no-reissue waiver from this run does **not** extend to it; it gets a proper brief.
- **R9 part two is queued and unbriefed, and its design is already settled** at Checkpoint 1: one home for a rate (a dated measurement carrying both currencies, its value, its date, its grade and its provenance); the person's home currency as a recorded ruling asked through the question queue, with the majority currency offered as the suggested answer; `VIVA_CURRENCY` never repurposed; the rate carried forward from its own date with that date disclosed. It does not ship until Vishnu rules on the one question a stand-in would not take: **what is owed a person when a figure true when it was spoken changes afterwards.**
- **The double hedge is struck from this cycle and deferred to whichever cycle takes the machine-written comparison form.** A doubled *about* is clumsy and honest. All three routes to fixing it now are closed: reading the model's words is a standing anti-goal, dropping the machine's term breaks X2, and moving the inexactness term into a whole reviewed sentence is a pack version and a render change rather than a line.
- **The per-hundred residual stands.** `ratio_of_spending` and its eight siblings are still written per hundred and a model can still wrap one in the word *times*, with nothing looking, because looking would mean reading the clause. It belongs with the comparison form above. Do not attempt a partial fix.
- **A count of matches now reaches a person as prose, with no id, no grade and no citation.** The vocabulary cap caveat says *"The first 40 of N label(s) a name reached are named here"*. That is compelled — the caveat's numbers must be true of what came back — and it pulls against the rule that the lookup emits no figure over the matched set. Recorded rather than repaired, and it is R8c's channel.
- **`bounded` does not guard what the Builder thought it guarded.** It checks axis-uniqueness within `cut` only — never within `selected`, never `cut` against `selected` — so `counted < len(names)` is constructible through the envelope. It is unreachable today because `_query_balances` filters `infos` so the selected account and the cut account are the same string. If the rule that no other read emits an `accounts` block is ever lifted, that assumption is what will be quietly wrong.
- **Ten Witness cases stand from this run, all open, all Vishnu's to authorise.** (1) Do the three matching tiers reach the counterparties people actually name, and does any tier return a list long enough that a model picks wrong — if tier 3 buys nothing, drop it, one function. (2) Does the disclosed window match the period a person named; any wrong boundary reopens the relative-date enum for the two cases it covers. (3) Do real brokerage statements print an account total our composition does not reproduce. (4) Is `mixed_vintage` live or theoretical on the real vault. (5) How often is F1's shape reachable in recorded transcripts. (6) Does removing *how does my spending compare to my income* cost a question people ask. (7) Does per-clause placement read worse than pooling on a real three-clause answer. (8) Does a live model take the good path on the direction refusal, or does the turn simply die. **(9) — promoted — how often does an answer state a per-account balance alongside any whole-ledger or narrowed total; failure is any occurrence, because each one is a false coverage claim already shown to a person.** (10) Does any real vault already hold a multi-currency brokerage account; any hit moves both R2 items from latent to live.
- **Process finding: two Builders read a fence bullet down to fit rather than escalating.** G1's refusal tag, and R1's *count what it covers is what the ruling forbids* — a true sentence about the wrong option, since suppression requires counting nothing. Both reads were defensible; both should have been stop-and-reports. It is a process defect and not a code defect, and it is the one finding of this run worth carrying forward.
- **Process finding, and it is about how the roles are wired: a Builder wrote into the Verifier's scratchpad and overwrote the script that produced the original F1 reproduction**, leaving a revert harness aimed at `runner.py` minutes before the verification was asked for. The Verifier rebuilt all four reproductions from first principles in a fresh directory, re-derived every expectation from the briefs, and confirmed the tree was in the intended fixed state — so nothing was falsified. The guarantee that nothing *could* be is gone for this run. Vishnu's to decide on.
- **A Director ruling made in Vishnu's absence that he may want to look at first.** `pack-v14` — a permanent id minted by a Builder to resolve a conflict between two of the Director's own rulings, ratified on the ground that a pack version's permanence attaches when a recorded reading cites it and none does; the undo is one directory until commit. The transfers caveat's replacement sentence, written by the Director because it asked for Vishnu's words. The removal of *how does my spending compare to my income*. The matching rule's three tiers ruled without the measurement the brief offered. And the deferral of R9 part two, which is the one place the Director declined to act on rulings Vishnu had already given.

## Out of the hosted Witness sweep — what the repair plan never covered

_The sweep of 108 sessions / 116 turns produced the seven-item repair plan whose items R1–R9 are now all
built and committed. The plan also named four things it deliberately did **not** cover. Harvested here before `briefs/what-the-spree-found-and-how-it-gets-fixed.md` was deleted, because they lived
nowhere else and one of them is now overdue._

- **`check_completeness` contradicts `debug.tiers` on the unidentified count, and it is live as of now.** The
  plan's own words: *"Not model-facing while the verb is dead; it will be the moment R4 lands, so reconcile
  before then."* **R4 landed (`202b320`)** — the grade hole was retired and `check_completeness`'s
  counts became `ACTIVITY`, so the verb is no longer dead and the contradiction is now reachable by a model.
  The reconciliation was owed before that commit and was not done. Nobody owns it.
- **`get_provenance` has still never produced provenance.** Every route failed in the sweep. One defect needs no
  model at all: it refuses `unknown_record` for the holdings sub-account id that `aggregate:net_worth` itself
  emits and cites. R9 part one removed that particular id from the tree — the `:Holdings` pseudo-account is
  gone — so the specific cause is closed, but the verb was never seen to work by any route and needs its own
  investigation before it can be planned. R9's Builder was explicitly forbidden from claiming to fix it.
- **Five figures the Witness doubts but cannot call wrong**, including a category total driven almost entirely
  by one purchase, and a fee total differing by roughly an order of magnitude between two surfaces. These are
  data questions rather than code questions; each needs Vishnu's judgement and a vault open in front of him.
- **No write path was tested anywhere in the sweep** — no ingest, no ruling, no rebuild, no reingest. Every
  finding it produced, and therefore every one of R1–R9, is read-side. The read side is the cheap reversible
  half by this project's own standing rule, which means the expensive half has never been swept at all.
- _Also recorded from the plan: the vault it ran against was a **rebuilt copy**, not the
  canonical path, so findings about mechanism carry over and findings about that data may not; and the vault
  held **zero tags**, so tag narrowing was untestable — including the tag half of what `517f20b` shipped._

## Out of the hole-declares-its-scope cycle (R1)

- **A scope written in a clause's literal words is unchecked, and that is the residual this cycle narrows to its sharpest point.** *"You spent {amount} on travel"* declares `whole`, binds a real total and passes. Closing it is Option 4 — a figure naming a slice may only be stated in a clause binding that slice's entity — rejected on a measured cost, not on taste: only three of the nine ways a set may be narrowed have an entity a hole could bind, so the rule would make every breakdown by subcategory, tag, currency and month unspeakable. **Witness case: do models bind an entity for what a clause is about, or write it into the words?** If mostly words, Option 4 becomes the question rather than a note.
- **Two figures and two entities of one kind in one clause can be swapped.** *"Your balance on {A} is {x} and on {B} is {y}"* passes with A and B exchanged, because each figure finds *an* entity of its kind with a matching value. Closing it needs positional pairing, and position is the sentence.
- **A holding's value is unspeakable, as a row and as a number, and it ships that way.** A holding is *in* an account rather than a slice of one, and no member of the narrowing vocabulary names an instrument, so a per-holding figure names no set a sentence can declare. Per-instrument only: the holdings read's own total and its total narrowed to one account still answer. Both escapes stay forbidden — no vocabulary member naming an instrument, and no boundary that names nothing filling a hole. **Chartered as its own item after R7 and R8: *what a holding is a slice of*.** It is entangled with whether a holding is an entity, and therefore with Option 4. **Witness case:** the rate at which per-instrument questions end in refusal, and whether the question had a reachable honest answer.
- **The period *value* is unchecked by the hole.** Set equality compares the axes and only the axes, and the vocabulary holds no entity for a span, so a sentence naming the wrong window binds a figure cut by `period` as happily as the right one. Slice 4 closed the one case where the declared span itself was false — a month group of a windowed read now declares that month intersected with the window — and left the value unchecked.
- **C3's period half is refused, not answered.** A monthly average whose divisor was the count of months a category had movements carries no span in its own clause, so the hole is unfillable and the turn refuses. **The span a figure's operands were measured over is a third object nothing computes** — not attested coverage, not the window asked for — and writing it as a `cut` would put a fact about the data into a vocabulary of narrowings. Restoring the capability needs a decision about where a measured extent lives on a figure, and it is its own brief.
- **L4 is narrowed, not closed, and the remainder is a new emitted fact.** The net-worth read's `dated` is today by decision, so a date hole bound beside its total is correct by the machine's own declaration; what the clause-local rule fixes is today's date leaking into a clause whose figure is months stale. The rest — *the net-worth read emits its stalest input as something an answer can refer to* — is now its own line in the plan document, beside the wording tail's *"see the point's own staleness fields"*.
- **Two pieces of deliberate dead weight, kept exactly as they are.** `check_completeness`'s per-account date figures can fill no hole and so can be in no clause; and the run-wide pool of days is written at two sites in the runner and read nowhere. Both were left untouched on purpose — the figures are another cycle's fence, and removing the pool is code this cycle had no brief for. The next reader who finds them should know they are inert rather than load-bearing.
- **Nothing stops a model typing "verified" into its own clause text.** Carried forward unchanged from the grade cycle; this cycle neither narrowed nor widened it.
- ***What a count is a count of* is chartered after R7 and R8, and R7 and R8a+R8b have now landed — so it is due, behind R8c.** A count of things found cites only what it found, so a count of none cites nothing and refuses at the citation gate — asking how many holdings you have on a vault with none is told nothing rather than told none. It is a class, not an instance (`accounts holding a balance`, both movement counts, `months these movements span`, `spending movements counted`), and the answer is not uniform: C3 divides money by a month count, so a wrong number there moves money. Sort them one at a time by *what would a wrong number here move?*; a sweep-and-replace is a rejection.
- **The companion design document ADR-013's Consequences deferred is now owed rather than waiting**, and chartered after R7 and R8, which have now landed — so it is due, behind R8c. Its debt grew again this run: two further comparisons a binding fails on, and a placement rule for the machine's own sentences. The hole its deferral named is closed; the renderer wrong by a hundred is R8's. What it owes has grown: four declarations compared pairwise, a rule whose unit is the clause, and a composition rule for boundaries through arithmetic.
- **`_agreeing` compares whole boundaries and is deliberately the strict direction.** Comparing cut sets alone would let two operands agree while differing in `unmeasured`, `unposted` or `accounts`, and would then need a composition rule for which disclosure the result inherits — arithmetic laundering a gap disclosure, which is the same family of harm as arithmetic laundering a scope. A refusal beats a probably-right inheritance.
- **Witness cases standing from this cycle.** (i) How often the `compute` inheritance rule refuses ordinary averages because two reads were filtered slightly differently — the brief's own most-likely-too-strict. (ii) The rate at which `wrong_scope` and `wrong_subject` fire, and whether those turns had a reachable honest answer. (iii) Whether a person reading a merchant-narrowed group's two sentences takes the number as the merchant's groceries or as all groceries — **withdrawn** when set equality closed it structurally, and recorded here so nobody re-derives it.
- **A standing correction the Director made on itself, four times, and it is worth keeping.** A number that cannot be re-derived from the tree does not enter a ruling, and certainly not a rejection line; an illustration in a ruling is measured with its instrument or it carries no numbers. Three counts and one illustration were withdrawn on that ground during this cycle.
- **The docs-track-the-code guard still walks the filesystem**, so a gitignored brief satisfies it. Proved by hand again: all four ids now in force — `speak-v12`, `tools-v14`, `speak-shape-v11`, `pack-v14` — are named in tracked documents, and so is `tools-v13`, which is released and was never in force. Its own brief item is still owed, and nobody touches it before then.

## Out of the grade-is-placed cycle (R4a+R5)

- **The mutation proof is 14 tests, not 18, and the report should say 14.** Emptying `stood_behind_*` while leaving `result.grade` intact fails eighteen tests by name, but five of the eighteen fail through a degenerate path — the test computes its own expectation from the same mutated `moment`, so it would fail for any edit to that pack family and proves nothing about the placement. The honest strengthening this cycle bought is 13 → 14.
- **Half of `test_an_answer_is_never_stood_behind_more_strongly_than_a_block_it_carries` is a lemma, not the guarantee.** The ladder-wide loop is a property of `weakest()` alone; the runner-side guarantee — that the answer's word is computed over a set containing the block's — is carried by the fixture assertions above it. A later reader must not weaken the fixture believing the loop covers it. The comment beside the loop now says so; nothing enforces it.
- **F5, widened slightly and still out of fence: `figure()` accepts a money-kind figure with no grade.** No emitter produces one today. What changed is the cost if one ever does: the block's grade now flows into the trailing sentence, whose reviewed words claim something of "every figure in it", so an ungraded money figure would be covered by a sentence that has not seen it. The fix is structural — refuse it at `figure()` — not a check at the speaking end.
- **Nothing stops a model typing "verified" into its own clause text.** The residual this cycle narrowed and did not close. A word list is a standing anti-goal, so the narrowing is all there is: the clause is authored before any read, so the model is betting on a grade it has not seen, and the machine's own sentence is now in the same answer to contradict it. Recorded in the tool-registry doc and in ADR-013 as well, because this file is gitignored.
- **The completeness caveat now says the same thing twice.** *"N counterparty(ies) have no category yet, so their categories are unknown."* True, and circular. The trailing clause was kept deliberately — the Builder's replacement, *"so what they were spent on is unknown"*, is false of the set, since `uncategorized_merchants()` covers employers, transfers and inflows — so what is left is a wording item, not a correctness one. A caveat reaches a person verbatim, so it is worth one line of somebody's attention.
- **A process deviation to see rather than to inherit.** That one-line revert was made directly by the delegated Director, not by a Builder from a brief. It is one string literal, reverted to the text at HEAD, and no test pins either wording — but code changed outside the build lane, and the record should say so once rather than have it discovered later.
- **The per-account date figures now fill no hole at all.** They exist to put a day into the run's ledger so a date can be spoken; a grade hole was the only hole they could ever have filled. Accepted at Checkpoint 1 and named here rather than glossed.
- **An answer whose only content is a date carries no grade sentence**, where it could previously have carried *"that is verified"* — a claim about a balance's evidence attached to a day. Read as a gain in precision, and recorded in case a live run reads it as a loss.
- **Witness case, standing from Checkpoint 1: does weakest-across understate in practice?** The next sweep counts answers where the answer-level grade is strictly weaker than the strongest money figure stated. If a person is routinely told less than the vault knows, the answer is a narrower placement rule and not a per-figure sentence — and it wants measuring before it is designed.
- **The next cycle proposing a fourth machine-placed sentence per answer owes a rule for the order and the total of them**, not just its own line. Set as a fence at Checkpoint 1. An answered turn now carries three.
## Out of the reads-that-say-no-scope cycle (R2)

- **W-R2-1: does a balances answer read as clutter now that it carries two scope sentences?** _(Reworded — the sentence it quoted is gone.)_ A balance narrowed by account says *"That counts only what is on ⟨account⟩"* under its own clause, and the answer may then say *"This answer covers 1 of the 5 accounts you hold, so it is not the whole of them"* once, after the clauses — and only where every stated figure declares exactly which accounts it covers. Both are true and neither contradicts the other; together they are wordier than either. Ruled at Checkpoint 1 that recording a true narrowing is never withdrawn on a readability finding — if it reads badly the defect is **where and how scope sentences are placed**, which is R8's. A person reading it is what settles it.
- **W-R2-2: does the new count figure on `list_movements` change which read a model reaches for?** The detailed read now emits a total, which makes *how many payments went to that counterparty* answerable from it — and may pull a model onto the row-returning read for questions the summary read answers more cheaply. A cost finding for R7 if it happens; one line to remove if it is not worth paying.
- **W-R2-3: R8's symptom is knowingly more frequent.** Every read that narrows now places sentences that no read placed before, and they are all appended after the answer's own words. This cycle made more of them exist and moved none of them. R8's brief opens by saying so, so the tail is not misread as a regression R8 introduced. **R4a+R5 adds one more**, unconditionally on every answer that states a graded money figure as a number in a sentence — so an answered turn now carries three machine-placed sentences and R8's tail is longer again. The fence set at that cycle's Checkpoint 1: whoever proposes a fourth owes a rule for their order and their total. **Answered in part by R8b:** the sentences are no longer appended after the answer's own words — each goes under the clause that bound its figure, and the accounts-covered claim is the one that stays at the end. Whether that reads better is Witness case 7 and is unrun.
- ~~**`compute` still hands back a total with no boundary.**~~ **Closed by R1**, in the direction this line predicted: identical operand boundaries are inherited, differing ones give a number over neither set that fills no hole of any kind, and a literal contributes no set and takes none away.
- **Income on a multi-currency vault is broken in two ways at once, both upstream of the tools.** `income_by_currency` attributes to a placeholder key where the accounts declare more than one currency: narrowing the income read by a currency the vault does hold comes back `ok` with no figures at all, so the read records what narrowed it in no figure; and the unnarrowed read declares that placeholder as the figure's own currency, which renders to a person as `? 500.00`. The second is a figure claiming something untrue, not merely a read saying too little. The tools' side of it — writing a slice only for a key the vault's own currency vocabulary knows — is what shipped here; the attribution is untouched.
- **A structural date check accepts non-ASCII digits, and it is I2's business.** `_is_iso_date` tests with `str.isdigit` and the new month parse with `int`, which also accepts a leading sign, so a key of Arabic-Indic digits passes as a calendar month and a scope sentence can be built from a string nothing validated. Surviving rather than introduced — the same acceptance sits under every date the tools compare — and recorded in the tool-registry amendment as well, because this file is gitignored and a defect recorded only here is one nobody who clones the repo can read.
- **Which grouping a list over the transactions read enumerates is undecided.** The block guard fails closed rather than guessing between accounts and months. The day that product decision is taken, the guard is the place it lands.
- **What names a row, when a row is not a slice.** A listed movement declares whole and carries no cut, because writing one would mean inventing a way of narrowing a set to a single movement, which the vocabulary forbids. So the read that exists to return rows still cannot fill a block-of-rows hole. Real, open, and not this cycle's.
- **The registry still runs a tool with no exception handling, against a module docstring that says a call never raises.** Untouched by design: wrapping a tool call in a blanket handler turns a programming error into a refusal a person reads, which is a design question. What this cycle bought is that the general test calls every read with every filter it honours, so a read that reintroduces the contradiction fails there rather than in front of a person.


## Out of the diagnosis cycle (R6)

- **W-R6-1: does the strict rule cover a useful share of the twenty-three?** The cause is spoken only when the *last* registered-tool result refused, so a turn whose last read succeeded says the verdict alone. Of turns that refused with an empty `diagnosis`, how many had an eligible read refusal earlier in the transcript? Answerable from the recorded model exchanges of a sweep, no new code. If it covers under half, loosening to the last refusal anywhere in the turn is a one-line follow-on brief — at the price of a cause that is true and no longer the reason.
- **W-R6-2: do multi-fault filter refusals happen at all, and what is inside them?** `invalid_filters` was removed from the slice because its account depends on what its payload happens to hold — two malformed window edges would have told a person that things they never asked about are absent from their records. A *wholly* vault-facing multi-fault call has no evidence behind it either way; if a sweep finds them, splitting the tag or speaking per-fault is a follow-on brief, and it touches a shared helper serving three reads.
- **W-R6-5: does `diagnosis_filter_unsupported` read as decoration?** Ruled in at Checkpoint 1 with the trade taken knowingly. Stripped of the caller's value it says the reading reached does not sort the records that way, which is either useful or near-nothing; a person reading it is what settles it, not an argument.
- **The rest of the reads' tags stay call-facing, including `not_a_balance_account`.** Promotion is one set entry plus one reviewed sentence, and by construction it is cheap; what it wants is evidence naming the tag, not a hunch. The gate is written beside the set in `envelope.py` and in the tool-registry doc's closing note — read it before adding an eighth.
- **`RunResult.diagnosis` is write-side and therefore permanent.** Its meaning is fixed: empty means *no cause was spoken*, not that no read refused. Anything counting it later has to read it that way.
- **R6, R3, R2, R4a+R5 and R1 are the first five of the spree plan's items; R7 and R8 are not in this tree.** R1 was blocked on R2 and is not any more — every read it compares against now declares — and R4a+R5 removes one of the two ways the plan's C3 produced a wrong number before the scope-check work begins.

## Out of the every-clause-carries-a-hole cycle (R3)

- **`supposed` is now load-bearing for thirteen tests that are not about it, and a tightening of `unfounded_stipulation` reddens all thirteen for the wrong reason.** A clause needs a hole, and the cheapest hole for a test that reads nothing is a value the person put into their own question — so those fixtures now carry a question text whose number the binding must repeat, whole. Measured rather than estimated: forcing that check to fail turns seventeen tests red, four of which are the pre-existing tests *about* `supposed`, leaving thirteen — nine in `test_speak.py`, three in `test_shape.py`, one in `test_tools.py`. The Checkpoint 2 ruling recorded eight plus one; the number is thirteen. Giving those tests a real read instead would be worse: it makes tests about correction budgets and tool tables depend on read machinery they are not about.
- **One of the thirteen has a cheap way out, for whichever cycle next opens `test_shape.py`.** `test_a_split_introducer_leaves_a_blank_line_above_the_block` only needs its introducer to *be* a clause; any hole does, and a `date` or `account` hole takes it off the question text in two lines. The protocol tests have no equivalent — they need a hole that binds with no read at all.
- **A split introducer renders a blank line above the block, and it is pinned rather than fixed.** The break the block opens with travels with the block, and the runner already puts one between clauses, so the two meet. `speak-shape-v8` teaches the merged form where this does not arise; the split form stays legal. Fixing it means touching `_written_out`, which was outside this cycle's bound.
- **Courtesy is now a refusal on the answering path, accepted at Checkpoint 1 and only a real run can price it.** "You're welcome", "let me look at that" and "hope that helps" cannot be authored; the turn refuses. What would reopen it is a Witness run showing a meaningful share of turns refusing where a plain reviewed sentence would have been *honest* rather than merely convenient — then the pack owes a machine-fired moment placed on a condition, never a key a model selects freely.
- **Viva cannot state her own definitions, and the reason she must not is unresolved.** *Are own-account transfers in my spending figure?* now refuses. `speak-shape-v8` copies v7's `spending` definition verbatim — money gone to somebody else, own-account transfers and settlements not in it — while the grouped spending read returns a total holding a category named `transfers`. Until a real vault settles which of M1 and `aggregate:spending` is wrong, a reviewed definitions sentence would be a human-approved falsehood. **That contradiction is a Witness case and belongs to whichever cycle owns that read**; it was named and deliberately not fixed here.
- **A `supposed` hole binds without any read, so the rule is narrower than it reads.** The hole holds a value out of the person's own question; it cites no record and carries no grade, and the existing suite already speaks *"The {trip} you mentioned is well within reach"* through one. So the shape this cycle exists to stop — a hedge beside a figure — is re-authorable at one remove wherever the question carries a number, and it is spoken rather than dropped. It is genuinely narrower (the question must contain the value whole, and the clause drops when it does not) and it is not nothing. Recorded in ADR-013's amendment as where the prose residual is widest; whether `supposed` should be admissible as a clause's *only* hole is a design question, not an edit.
- **The words around a hole are still read by nobody.** A clause can now bind a real spending figure and describe it falsely in the model's own words. That is ADR-013's residual, and **R1 narrowed it rather than closing it**: a sentence can no longer be about a set its figure was not taken over, nor state the number of a thing other than the one it names through a hole, but a clause naming its subject in its own literal text still passes. See the R1 section above.

## Out of the blank-filter cycle

- **`nature` comes back, with a wording authored for it.** *Show me my transfers* is a real question and nothing else offers it. What it needs is a boundary sentence in a new pack version and a member of the boundary vocabulary — the withdrawal was taken so the vocabulary is clean to return to, not because the filter is unwanted. The three `_SUPPORTED_FILTERS` entries, `QUERY_LEDGER_PARAMS`, `_movement_passes` and a tools version come back with it.
- **A punctuation-only description is its own counterparty, and an answer can name one a follow-up bounces.** `"!!!"` survives every guard this cycle added: it becomes its own merchant group key, is listed in the counterparty vocabulary, **mints an entity** — and is then refused as a filter, so a person handed that name back cannot ask about it. Identical at HEAD; this cycle neither created nor widened it. Whether such a label joins the `Unnamed` residual or stays its own group is a decision, not a repair, so it wants its own brief.
- **`Unnamed` is collision-proof for a reason the record should state correctly.** It cannot sit on top of a real counterparty because every write into `merchant_categories()` routes through `normalize_merchant`, which lower-cases, and the label is not lower case. The build justified it by analogy to `Uncategorized`; the analogy is wrong — a collided `Uncategorized` is a working handle, while a collided `Unnamed` would be accepted as a filter and return zero. The decision is right and the stated reason was not. Nothing pins the lower-casing to the label: if `normalize_merchant` ever stops lower-casing, the guard goes silently.
- **The docs-track-the-code guard cannot see the failure it exists to catch.** `test_no_document_names_an_older_version_than_the_one_in_force` walks the filesystem, so a **gitignored** file naming the new version satisfies it — this cycle was green here and red on a fresh clone, on `tools-v11` named by nothing but the brief, and that is exactly the release blocker the test is for. Its own module docstring says a gitignored file is out of reach of every test in this repo, which is false of `_prose()`. The fix is `git ls-files` rather than `rglob`, which is executable code and therefore its own cycle; until then the Steward proves the version pin the restricted way by hand — done again, where all five of that cycle's families were stale in tracked documents and green here. **Ruled at Checkpoint 2 of that cycle: it gets its own brief item next cycle, and nobody touches it before then.** An instrument that lies in the direction of passing is worse than no instrument.
- **`bounded()` still raises where the registry's module docstring says a call never raises.** This cycle removed the two paths that reached it — the blank merchant key, loud and quiet — and settles nothing general. Whether that guard should refuse rather than raise, and what else reaches it, is untouched and the docstring still states the contract absolutely.
- **Ingest still posts a movement with a blank description, with no non-empty guard.** The read side now names such money rather than dropping it or raising on it; the write side that produces it is untouched, which was the fence this cycle drew. The real vault holds zero such rows today — the shortest description in it is eight characters — so this is latent, and it is worst in the vaults where the blank rows carry the most money, since a blank group only reaches the raising path when it ranks in the top ten.

## Out of the list cycle

- **The vocabulary count answers for nothing, and is declared as the wrong kind of number. Shipped knowingly on Vishnu's ruling, and it has its own cycle.** `query_ledger`'s `vocabulary` mode returns how many labels a vault holds with no grade and no `record_ids`, so *"why do you say five?"* comes back with nothing at all — the one question this product exists to answer. It is also declared `kind=ACTIVITY`, and a count of the vault's own labels is not that: it is a number about the person's ledger that happens to rest on ledger events rather than on documents. **`ACTIVITY` did widen** — from *what the agent did* to *what it did or holds on record* — and this item is untouched by that widening, because the question here is not the agent's paperwork but the person's ledger. Either `ACTIVITY` widens again or a fourth kind exists; that is still a design call, and the sorting test to answer it with is *what would a wrong number here move?* Recorded in `docs/agent-toolset.md` as well, because this file is gitignored and a known defect recorded only here is one nobody who clones the repo can read.
- **A row still cannot cite its own records** — fenced out of this cycle deliberately, and now visible ten at a time. One grade is computed over a whole read and stamped on every figure, and every per-group figure carries the entire read's record set, so a row for one subcategory cites documents holding none of its movements. Pre-existing and true of category and merchant groupings before any list existed; what changed is that ten figures now face a person at once, each answering *"why do you say that?"* about the whole read. A per-row grade is impossible to state honestly until this moves.
- **A subcategory is still not filterable.** Naming a group as a figure's scope was the whole of what this cycle did about it: a person handed `groceries / produce` back as a filter is refused, and the refusal names what is filterable. Making the pair a filter is a read-side capability with its own weight, and it is what the rows now invite people to ask for.
- **Nothing here decides what a breakdown looks like on a screen.** The block is the minimum a text answer needs — a line per figure, name and amount, grade once above, the read's own tail sentence below. The presentation layer is untouched and nothing built here should be read as having settled it.

## Out of the Witness run

- **A wrong number reached a person, graded `verified`, and the quantity check cannot catch this class of error at all.** A shape asked for a count of subcategories; the binding filled it with a figure whose own `what` says it counts *movements*; the answer was out by roughly an order of magnitude, and the true group count sat in a caveat inside the same sentence. The guard passed it because it compares two tokens and both read `count` — and `quantities_of('count')` returns exactly one value, so every countable subject in the product, movements and groups and accounts and documents alike, declares the same word. **For that whole kind the declaration carries no discriminating information**, and the property `shape.py` states as its purpose — that a hole says what its number is of, so a real figure about one thing cannot be put where another belongs — structurally cannot hold. The mechanism is otherwise sound: in the same run a money hole whose bound figure measured something else refused correctly. Pre-existing, not caused by `af2241f`. **This is the second time a wrong number has reached a person** — ADR-013's residuals record the first, and say the guarantee is not whole until such things are closed. It was to come ahead of the list capability and did not: the list shipped first, and the vocabulary read shipped with it emits **one more countable subject declaring the bare word `count`** — how many labels a vault holds, beside how many groups its spending falls into, which is the exact pair of numbers the wrong answer confused. Next cycle, and now with two subjects in the same sentence to tell apart.
- **The likely shape of that fix already has a precedent in the same file.** `quantity.py` builds `ratio_of_<kind>` from a helper because a proportion of one thing is not a proportion of another; a count of one thing is not a count of another by exactly the same argument. `count_of(kind)` is the symmetric move. It is an addition to a closed list whose every member is permanent, not a redefinition of `count`, so the old word keeps meaning what it meant — but every emitter of a count and every hole asking for one has to say which, which makes it a design cycle rather than an edit.
- **A clause may not carry a digit, and a hole name may — but the check reads inside the braces.** `Clause` rejects the text if any character is a digit, scanning `{cat1}` along with the words, while the hole-name grammar permits digits and the docstring says digits are banned *outside* a hole. This fired live: a correction told the model to make holes and declarations agree, it renamed them with numeric suffixes — the obvious move — and the next turn died, with a refusal saying it wrote a digit in its own words when it had not. Any question inviting enumeration invites numbered hole names.
- **Internal account keys reach prose a person reads.** Caveats embed identifiers of the form `acct:<institution>:<digits>`, carrying a real institution name and a digit fragment into the sentence under the answer.
- **A question about "last month" resolved to windows more than a year from the run date.** The reads disclosed the mismatch through attestation caveats rather than returning silently empty, so the machinery was honest about it; the window selection was still wrong.
- **The `choose_the_quantity` residual is still untested.** No hole declared a disallowed quantity during the run, so the correction that presumes the kind is right and the quantity wrong never fired. It stays a named residual.

## Out of the schema-and-repair cycle

- **The schema half has never met a live model, and one run settles it.** A tool schema is a hint: no `response_format`, no `tool_choice`, no strict flag at any depth, so a provider that flattens or ignores the five-branch `oneOf` hands the model back roughly the form it had before. Nothing in this cycle could test that without spending money. A local run passed it cleanly — no plain-kind hole carried a `quantity` in any shape call, and neither origin failure recurred — but that is a different model at a different provider from the one `.env` routes to. **The hosted run still owes an answer, and it needs Vishnu's own ruling to spend**: the Witness will not take a relayed authorization for real money. The brief already named the consequence if the alternation is reliably ignored — the schema change buys nothing, and this becomes the correction fix alone with the form left honest and inert.
- **The counters are in a debug reader, not the eval, and nothing watches them.** `viva.debug.speak` prints shapes authored, shapes rejected and the repair each refusal named, which is two of the four subjects ADR-013's Consequences promised the eval. The other two — holes that could not be filled, clauses dropped — are counted by nothing. The two that exist appear only when a person opens the vault and runs the command; no build fails on them and no release is compared against the last. ADR-013 now says this in place rather than claiming the consequence discharged.
- **The correction budget stays at one, and the number that would move it now exists.** B4 was ruled out because the observed failure was a correction pointing the wrong way, not too few of them. What would reopen it is a *repaired* correction failing on a second attempt more than rarely — visible now as a turn whose second authored shape is rejected again. `shapes()` reports how often, not whether the repair worked; deriving the second from the same payloads is small and unbuilt.
- **`FROZEN_SPEAK_PROMPTS` is a second pin table, kept by hand.** `prompts-as-files.md` says `released` in the manifest is *the only pin table; the four test-file copies read from it* — four do (`test_prompt_library`, `test_tools`, `test_persona_pack`, `test_interview`), and `test_speak.py`'s does not. It keeps growing: two digests in two places one cycle, three the next. Pre-existing, not caused here, and it is one comprehension away from the others.
- **The refusal sentence still ends with advice that could not have helped.** Fenced out of this cycle after the Fact-checker refuted the premise: the sentence already owns the fault in its first clause, and what remains is a half-sentence asking the person to rephrase, which does not stop a model putting a quantity on a hole that cannot take one. (The example that framing was written against was a quantity on a *grade* hole; there is no grade hole now, and the point survives on every other hole type.) Correcting it costs a tenth persona pack. Reinstated on one word from Vishnu.

## Out of the card-paid cycle

- **The next cycle, already ruled: `implication_of`, and the structural guard with it.** The fourth site deciding direction from a raw posted sign is `product/viva/ledger/projection/merchants.py:152` — `implication_in(record, inflow=m.amount > 0)` — so on a liability a purchase reads as an inflow and the counterparty's implication is picked the wrong way round. At four sites the allowlist cost of the AST guard the brief deferred stops looking like the larger one, so the guard ships with the fix. Until it does, three documents carry a dated clause naming this site — **M2** in `design-invariants.md`, the naming paragraph in `honest-aggregates-and-the-learning-loop.md`, and §4.1 of the transaction intelligence spec — and each says the clause is deleted when the fix lands rather than made true.
- **"The empty-kind raise is unreachable" must not enter the record as settled.** The Verifier was right in conclusion and wrong in reason: `_enumerated` does guarantee `m.kind`, but the guard sees `kind_for(m)`, and `streams_report.py`, `agent/observe.py` and `enrich.py` each build their own `kind_for` doing `account_info(...).kind or ""` inside a bare `except`. The empty string is reachable in principle, through a failed or empty account lookup, and simply does not fire on this vault because every account carries a modelled kind. Its blast radius is a hard stop on the streams report and on the maintenance agent's wake, not only on an ingest — worth knowing before a vault with an unmodelled account kind meets it.
- **One merchant is asked two arrangement questions about one charge and one credit** (Witness, lower severity). Not caused by this diff and not a regression — the same merchant carried both hypotheses before it, with the two labels swapped — and both are below the measurement floor. It is *direction is part of the subject* meeting a one-charge-one-refund relationship, and it reads oddly. A wording cycle, not a defect.
- **A cycle check must add the edge and import it, not co-import the two ends** — and where that lesson lives is Vishnu's call, because it changes a role file. A brief claim about module placement was marked verified by a script that imported both modules in one process, which cannot surface a cycle that does not exist until the new edge is added; the Builder then hit `ImportError` in every entry order, 48 test modules failed at collection, and four source files were reverted — one full build round trip. Recommendation: one line in `.claude/agents/fact-checker.md`, since it is a rule about how a claim is checked rather than about the loop or its gates. `WORKFLOW.md` if you would rather it bind every role.
- **Style-pass debt in the touched test files, left alone deliberately.** Several long-standing docstrings in `test_streams.py` and its neighbours narrate old bugs (`M`'s tells the `str - str` story), which `STYLE-COMMENT-PASS.md` would cut. Rewriting them in this cycle would put unrelated prose in a bug-fix diff; the claim they carry is already held by a named test (`test_dates_arrive_as_iso_strings_and_intervals_still_work`). A pass of its own.



## Drift check — verified, swept, and what is left


### The drift check's live code defects — what is still live

_D18 shipped in the "a debt is not a balance" cycle and is struck; what came out of it, **D18a, is closed by R8a** — a value denying the direction its quantity asserts fills no hole asking for that quantity, so the negative debt is refused rather than spoken. The line below is kept for the trail; what remains of it is a Witness question, not a defect._

- ~~**D18a — OPEN, and shipped knowingly: the live path speaks a credit as a negative debt.**~~ **CLOSED by R8a**, and what is left of it is Witness case 8: whether a live model, refused its debt clause, writes the credit clause that keeps the turn answerable or simply loses the turn. The original entry follows. An overpaid card comes back `-50.00` declaring `owed`, and a model binding it into the debt clause says *"You owe -USD 50.00 on <card>"*. The sign is right and the sentence is not one anybody should read. Disclosed and accepted at checkpoint 2 rather than sent back to design: the wrong *word* is gone and only the wording remains, the shape is authored before the read so nothing in this repo can settle which clauses a live model writes, and fixing it at the moment of rendering was ruled out. Guarded by `test_shape.py::test_an_overpaid_cards_sign_survives_into_what_is_spoken`, whose docstring says it tests the sign and not the wording. What would move it: a Witness run on a real overpaid card, or a design cycle on how a shape carries a case it cannot name before reading. **It now has a tracked home** — `docs/the-surface-cards.md`'s status names it, reproduced and dated — because this file is gitignored and a known defect recorded only here is a defect nobody who clones the repo can read. The public half is unfiled: a Reporter issue is Vishnu's word to give, and no one has given it.
- **E1 — the bench answer key joins on model-written labels and collapses.** `keybuild` `setdefault`s into a `(type, normalized label)` bucket, so every line item after the first is discarded uncounted, while the extraction prompt orders the model to label them all identically *and* to emit them all. Executed: two drafters reading perfectly and identically gave **35 claims → a 5-entry key**, and a candidate that omitted **ten of eleven transactions** scored `accuracy 1.0, recall 1.0`. `extraction-and-confidence.md` measured this and ruled the identity is (value, page, region); the code never changed. Page, region and a namespaced group are already written onto every claim and read by nothing. Until this is fixed, no figure the harness produces is a measurement of the candidate.
- **No Witness run stands behind the "a debt is not a balance" cycle (`a7ca3b1`).** Six cases were named at verification and none was run; no vault was opened and no model was called anywhere in it. The load-bearing one is whether a live model, given `speak-shape-v6`, actually writes the two clauses — one for a balance and one for a debt — and then drops the one the read did not support. That pattern is what buys back *"what's my card balance?"*; without it the ordinary question comes back as a refusal rather than a number, which is the accepted interim cost and is untested against any real model. A run would also say how often D18a is met in practice.
- **Five Witness cases are named and unrun, and four cost no model call.** They belong to the record cycle, which opened no vault. The one worth running first is **W4 — count every correction applied to a movement that had already posted, across the real vault.** ADR-007's amendment says the orphaned-overlay flaw is *latent*, on the argument that the only correction path acts on a held, unposted statement. If W4 comes back above zero, that sentence is false: B3 stops being an open design question and becomes a live defect with orphaned overlays in a real vault, and the amendment needs correcting the same day.
- **`docs/local-first-storage-and-crypto.md` still presents SQLCipher-encrypted SQLite as the single system of record.** There is no `sqlite` and no `sqlcipher` anywhere in `product/` or `core/`; the vault is a hash-chained encrypted JSONL log plus content-addressed blobs. Bigger than anything that sweep touched and outside its fence — the reading guide flags it, the document itself is untouched, and rewriting it is its own cycle. It is the standing question's answer for the cycle that names it: a document says a storage architecture is built that nothing built.
- **A denylist judgement for Vishnu: real bank names are already in the tracked record.** `docs/from-your-words-to-the-ledger.md` uses a real bank in an example question one line above a line the record cycle edited, and it is not the only site — `docs/data-model-spike-findings.md` names four institutions as belonging to *"one person's corpus"*, `docs/document-preprocessing.md` names fixtures after one of them, and `docs/the-question-queue.md` puts one in an example with an amount. None is on `.denylist`. Either that is a deliberate line (a large bank is not identifying) or it is an oversight across several commits; the Steward did not edit them, because it is a policy call and it is public speech. If they are to go, the entries go on `.denylist` too, or the ritual cannot catch the next one.
- **`freeze-key` is not idempotent** — it re-folds an already-folded key, so entries grow 1→2→3→4 and each pass commits a different hash, breaking the design's "prove they used the identical key".


### Corrections to the audit's own citations

- **E6–E9 all name the wrong file.** Every cited line is in `benchmark-harness-architecture.md`, not `benchmark-harness-design.md`; the line numbers are exact. The design doc was read in full and is clean.
- **A8 and D12 cite `CLAUDE.md`, which is gitignored** — no verifier, no lint and no reader of this repo can see it.
- **Three findings are wider than filed:** D1 had a third stale site (stale by *two*), D3's true count is thirteen with eleven downstream repetitions across five files, and D4's count is also wrong in `questions.py`'s own docstring — which a docs-only audit could never find.

### D12 · refuted

`CLAUDE.md` is gitignored by design, so nothing here can be repaired or linted for it. The finding's numbers are wrong regardless: `.claude/agents/` holds **eight** files (`fact-checker.md` landed since, and the audit's list of seven omits it) and `WORKFLOW.md` names the issue gate as a **fourth** checkpoint. The two tracked sources already agree at 8 and 4. The real question underneath: the project's most-read instruction file is the one file its verification cannot see, and it demonstrably disagrees with `WORKFLOW.md`. Either the durable half moves into a tracked file, or a fact is stated in a tracked file first and only referenced there.

### Cycle 3 — CLOSED, and what it left behind

The brief *what is owed when the code overrules the record* answered the question the ten
findings shared: when a cycle makes any claim false — a status line, a requirement, a
mechanism, a promise, or a thing a document says is built and isn't — that cycle corrects it,
and the Steward asks it out loud at the commit gate. The rule lives in `WORKFLOW.md` beside
the status-claim rule it widens, and in `.claude/agents/steward.md`.

**The record half is done.** Three of the ten (D19, A4, D20) were already reconciled by `697b6a4`. The remaining seven (A3, A6, D10, D17, B1, B2, B3) are corrected at
their sites; verification found five more sites the brief's table missed — `design-invariants.md`
T3/T4/T7, `positions-and-investments.md` in three places, `from-your-words-to-the-ledger.md`,
`implementation-roadmap.md`, and the Steward's own front matter — and the ship-time pass found
one more, the old fourth rung of the ranking ladder still standing in
`from-your-words-to-the-ledger.md`'s build-log section. All corrected. The four ADRs are amended
with dated notes and zero deleted lines.

**What the record half did NOT close, and is still open:**

- **B3 — the identity scheme is recorded, not decided.** ADR-007's amendment says what the code
  does and says explicitly that the question is open. A movement key carries the amount and the
  description, so correcting a posted movement changes its identity and orphans every overlay on
  it — the exact flaw ADR-007 named when it rejected content-derived ids. Latent only while the
  sole correction path acts on a held, unposted statement. **Witness case W4 above is what would
  falsify "latent".** Needs its own design cycle: keep content keys, or restore the hybrid, and
  what a correction owes the pointers aimed at it either way.
- **D17 / ADR-005 — the dual wrap is a build item now, not a doc item.** The record says a lost
  passphrase is a lost vault, in five places. Nothing recovers a vault. Whether the second wrap
  is a keychain wrap, a printed recovery phrase, or both, and when it lands, is undecided.
- **B1 — anchoring has never run.** No OpenTimestamps call, no RFC 3161 call, no periodic job.
  ADR-004 explicitly rejected "local chain now, anchor later", and every unanchored day is
  history that stays self-attested forever. It is Slice 12's work in the roadmap and nothing
  schedules it.
- **B2 — reconstruction was never ratified.** The ingest path stores no model request and argues
  the request is rebuildable from the stored document plus the immutable prompt version. That is
  a real position and no ADR has taken it. Either build the capture or amend ADR-003 to accept
  reconstruction, with the byte-fidelity cost stated.
- **A5 — net worth's `complete` disagrees with the figure's `whole`.** A `Liabilities:` account
  with no cash movements produces no line and no `missing` row, so a point can read
  `complete = True` while omitting a known debt. Fenced out of the record cycle by name; still a
  live design question.
- **A1's payload gate** — "ruled option D, nothing built", citing an audit document that is not
  in the repo. Nobody can scope it until someone says what it was.


### Still owed from the sweep itself

- **A7** — `pending_questions` omits the `as_of` default its two siblings have, so a declined expectation question is counted in `pending` and absent from the pending list (reproduced: count 2, list empty). No production caller today, but the state that trips it is the most ordinary answer there is, since "no, I don't have it" *is* a decline. The structural fix is extracting the eight-builder block that is copied three times, so "built by the same builders" is true by construction.
- **D13's ruling** — whether `spending_by_currency` goes on `counts_as_spending`, is retired, or is renamed. It omits every card purchase and is what three callers print as the headline; this is the unresolved **R5**, and `test_nature.py` currently asserts the divergence.
- **A8 / A9** — narrowing a doc's promise and widening a lint are different repairs with different costs. A8: `CONTINUE_INSTRUCTION` and the ingest reader's two retry suffixes are model-facing text in `.py`, and the ingest retry is the only retry path without a version (`reply.py` shows the pattern done right). A9: `registry.py` declares nine released prompt ids the lint misses on the `extract-` prefix — a one-line fix that goes red immediately, so it needs an `extract` family or a stated exemption.
- **`docs/README.md` enumerates four status values that the folder stopped using.** It says every doc carries **Draft / Stable / Living / Superseded**; the actual headers include *Implemented*, *Implemented in part*, *BUILT*, *Partially implemented* and *Design note*. Pre-existing, not made false by any recent cycle, and a one-paragraph fix — either the list grows a build-state axis or the headers stop inventing one.
- **`VISION.md` has no slot in the reading guide.** It landed at the repo root and `CLAUDE.md` points at it, but `docs/reading-guide.md` — the only place document order exists — does not list it, and neither `README.md` nor `docs/README.md` links it. Found in the docs pass of the next cycle; not fixed there, because where it sits in the recommended path is an editorial call, not a typo.
- **The bench's other two:** the `_system_metrics` dead branch found in the cold-read audit and never fixed, and the `find_corroborating_legs` silent truncation at 12 items that the same audit asked to at least log.
